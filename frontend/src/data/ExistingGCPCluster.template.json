{
  "title": "Import Existing GCP Cluster",
  "fields": [
    {
      "name": "credentials",
      "type": "group",
      "label": "Credential Configuration",
      "fields": [
        {
          "name": "credential_name",
          "type": "text",
          "label": "Credential Name",
          "helpText": "A friendly name to identify this credential",
          "placeholder": "e.g., gcp-account"
        },
        {
          "name": "service_account_json",
          "type": "file",
          "label": "Service Account JSON",
          "helpText": "Upload the GCP service account key JSON file",
          "required": true
        }
      ]
    },
    {
      "name": "general",
      "type": "group",
      "label": "General",
      "fields": [
        {
          "name": "googleProjectId",
          "type": "text",
          "label": "Google Project ID",
          "helpText": "Your Google Cloud project ID (not display name)",
          "required": true,
          "placeholder": "e.g., my-project-123"
        },
        {
          "name": "description",
          "type": "text",
          "label": "Description",
          "helpText": "Optional description for this cluster",
          "placeholder": "Enter description"
        }
      ]
    },
    {
      "name": "location",
      "type": "group", 
      "label": "Location",
      "fields": [
        {
          "name": "region",
          "type": "select",
          "label": "Region",
          "helpText": "Select the region where your cluster is located",
          "options": []
        },
        {
          "name": "zone",
          "type": "select",
          "label": "Zone",
          "helpText": "Select the specific zone (optional if region is selected)",
          "options": []
        }
      ]
    },
    {
      "name": "cluster",
      "type": "group",
      "label": "Cluster",
      "fields": [
        {
          "name": "clusterName",
          "type": "text",
          "label": "Cluster Name",
          "helpText": "The name of your existing GKE cluster",
          "required": true,
          "placeholder": "Enter existing cluster name"
        }
      ]
    }
  ]
}
