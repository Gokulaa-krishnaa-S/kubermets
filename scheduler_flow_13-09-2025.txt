main()
│
├── initial_collect_all(scheduler)
│   │
│   ├── get_active_clusters()
│   │
│   └── collect_cluster_data(cfg)   # for each cluster
│       │
│       ├── get_missing_windows(cluster_id)
│       │   └── get_latest_timestamp(cluster_id)
│       │       (calls helper.round_down_time)
│       │
│       └── collect_window(cluster_cfg, start_time, end_time)   # for each window
│           │
│           ├── fetch_kubecost_window(...)
│           │   │
│           │   ├── fetch_multi_aggregation_data(...)
│           │   │   (calls formatter.format_kubecost_window)
│           │   │
│           │   ├── build_mapping_tables(...)
│           │   │   (calls helper.parse_allocation_key)
│           │   │
│           │   └── fetchNodeData(...)
│           │       └── fetchPodData(...)
│           │           (calls formatter.format_kubecost_window)
│           │
│           └── send_snapshots_to_backend(...)
│               (calls formatter.format_kubecost_response)
│
└── schedule_cluster_jobs(scheduler, clusters)
    │
    └── collect_and_reschedule(cluster_cfg, scheduler)   # scheduled for each cluster
        │
        ├── collect_cluster_data(cluster_cfg)
        │   (same as above)
        │
        └── calculate_next_run_time(cluster_id)
            └── get_latest_timestamp(cluster_id)